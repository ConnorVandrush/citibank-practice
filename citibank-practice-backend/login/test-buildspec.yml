version: 0.2

phases:
  install:
    commands:
      - cd citibank-practice-backend/login
      - pip install -r requirements.txt

  build:
    commands:
      - echo "Running Login service tests..."
      - JWT_SECRET=test-secret PYTHONPATH=. pytest -v
