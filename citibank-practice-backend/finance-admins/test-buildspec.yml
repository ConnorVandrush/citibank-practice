version: 0.2

phases:
  install:
    commands:
      - cd citibank-practice-backend/finance-admins
      - pip install -r requirements.txt

  build:
    commands:
      - echo "Running Finance Admins service tests..."
      - PYTHONPATH=. pytest -v
