version: 0.2

phases:
  install:
    commands:
      - cd citibank-practice-backend/managers
      - pip install -r requirements.txt

  build:
    commands:
      - echo "Running Managers service tests..."
      - JWT_SECRET=test-secret PYTHONPATH=. pytest -v
